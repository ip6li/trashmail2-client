doctype html
html
    head
        title= title
        meta(charset='utf-8')
        meta(name="viewport" content="width=device-width, initial-scale=1.0")

        meta(name="description" content="Wegwerf Mailadressen sofort ohne Anmeldung. Sie bekommen hier temporäre E-Mail Adressen, die 3 Stunden gültig sind. Innerhalb dieses Zeitraums können Sie eingehende E-Mails lesen.")
        meta(name="keywords" lang="de" content="Trashmail, Wegwerf Mail, Einmal Mail, Email, Kostenlose Email Adresse, Email Adresse")
        meta(name="keywords" lang="en-us" content="trashmail, one time mail, disposable mail, discard mail, free email address, email address")
        meta(name="csrf-token" content!=csrfToken)
        
        != styles_jqueryui

        != styles

    body
        div(id="main")

            block navigation

            block header

            block form

            block mails

            block footer

        != jqueryjs

        != jqueryuijs

        block noscript

        block strings
        
        != base64js

        != qrcode

        != validator

        != mainjs

